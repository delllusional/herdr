[package]
name = "dontspeak-voice"
version = "0.2.0"
edition = "2021"
publish = false

[dependencies]
fs2 = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
wait-timeout = "0.2"

[profile.release]
strip = true
lto = "thin"
